import { createMuiTheme } from "@material-ui/core";

const Theme = createMuiTheme({
    palette: {
        type: 'dark',
        primary: {
            main: '#34BDDF',
        },
        secondary: {
            main: '#f9a825',
        },
    }
});

export default Theme;